#include <stdio.h>
#include <string.h>

int main()
{
    char str[1000];
    int i;

    printf("Enter the String: ");
    scanf("%s", Str);

    printf("Length of string is %ld", strlen(str));

    return 0;
}
